head	1.7;
access;
symbols;
locks;
comment	@ * @;


1.7
date	99.01.06.12.13.01;	author schj;	state Alpha;
branches;
next	1.6;

1.6
date	99.01.06.12.07.49;	author schj;	state Alpha;
branches;
next	1.5;

1.5
date	97.09.09.08.42.42;	author schj;	state Exp;
branches;
next	1.4;

1.4
date	96.11.05.15.07.59;	author schj;	state Alpha;
branches;
next	1.3;

1.3
date	95.12.17.21.59.50;	author schj;	state Exp;
branches;
next	1.2;

1.2
date	95.11.14.20.37.35;	author schj;	state Exp;
branches;
next	1.1;

1.1
date	95.11.14.20.30.14;	author schj;	state Exp;
branches;
next	;


desc
@marquardt main routine
@


1.7
log
@Version: 0.99g
@
text
@/* $Header: /home/schj/src/vamps_0.99g/src/maq.lib/RCS/marquard.c,v 1.6 1999/01/06 12:07:49 schj Alpha $ */
/*  $RCSfile: marquard.c,v $
 *  $Author: schj $
 *  $Date: 1999/01/06 12:07:49 $
 */
 
#include "marquard.h"
#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <float.h>

#ifdef HAVE_LIBSLANG
#include "slang.h"
#endif

#ifndef DBL_EPSILON /* Should be in float.h */
#define DBL_EPSILON 1.0E-12
#endif

double *inp = NULL;
double *out = NULL;
double **jjinv = NULL;
double *par = NULL;
int *fixed = NULL;
double *rv = NULL;
char	funcstr[1024]="nothing";
int	maqverb=1;
double diffac = 1.01;
int	it; /* Number of iterations */
double  fpar;
int maq_n;
int maq_m;
int maqrun();

#ifdef HAVE_LIBSLANG
SLang_Name_Type Maq[] =
{
  MAKE_VARIABLE(".maq_n", &maq_n, INT_TYPE, 0),
  MAKE_VARIABLE(".maq_m", &maq_m, INT_TYPE, 0),
  MAKE_VARIABLE(".maq_verb", &maqverb, INT_TYPE, 0),
  MAKE_INTRINSIC(".maqinit", maqinit, INT_TYPE, 2),
  /*I:maqinit
   *@@ Int maqinit(int m, int n)
   *
   * Initializes the fitting system. n is the number of
   * parameters to use, m the number of points used in the
   * fitting process.
   *
   * Returns 0 on success, -1 on error
   */ 
   MAKE_INTRINSIC(".maqend", maqend, VOID_TYPE, 1),
  /*I:maqend
   *@@ Void maqend(int maq_m)
   *
   * Cleans up after @@maqinit@@
   */ 
   MAKE_INTRINSIC(".maqrun", maqrun, INT_TYPE, 0),
  /*I:maqrun
   *@@ Int maqrun()
   *
   * Runs the fitting procedure with the current settings
   * The S-Lang function @@Int fitf(Array par[], Int m, Int n)@@ which
   * sets the @@rv[]@@ array should be defined and working. It should return
   * 0 if successfull and 1 if the function could not be resolved with
   * the supplied values for @@par[]@@.
   * 
   * Returns 0 on success
   */ 
  MAKE_INTRINSIC(".maqhead", prhead, VOID_TYPE, 0),
  /*I:maqhead
   *@@ Void maqhead()
   * 
   * prints startup options of the @@maqrun@@ procedure to
   * stderr
   */ 
  MAKE_INTRINSIC(".maqtail", prtail, VOID_TYPE, 0),
  /*I:maqtail
   *@@ Void maqtail()
   * 
   * prints exit info of the @@maqrun@@ procedure to
   * stderr
   */ 
  SLANG_END_TABLE
};

extern int sluser(double *npar, int mmm, int nnn);
#endif

static void 
Jacobian(int m,int n,double *jpar,double *rv,double **jac)
{
  int i, j;
  double d, p;
  double *rvcopy;

  rvcopy=(double *)m_memory((void *)NULL,(m+1)*sizeof(double),PROGNAME);
  for (i=1;i<=m;i++) /* Make copy of array */
  	rvcopy[i]=rv[i];

  for (j = 1; j <= n; j++) {
    if (!fixed[j]){
    d = jpar[j] * diffac;
    p = jpar[j];
    jpar[j] = d;
    }else{
    d = jpar[j];
    p = jpar[j];
    jpar[j] = d;
    }
    if (Funct(m, n, jpar, rv) != 0)
      (void)fprintf(stderr,"%s: function evaluation error\n",PROGNAME);
    jpar[j] = p;
    for (i = 1; i <= m; i++)
    if (!fixed[j])
      jac[i][j - 1] = (rv[i] - rvcopy[i]) / (d - p);
    else
      jac[i][j - 1] = 0.0;
  }
  for (i=1;i<=m;i++) /* restore original */
  	rv[i]=rvcopy[i];

  free(rvcopy);
}



static void HshReaBid(double **a,int m,int n,double *d,double *b,double *em)
{
  int i, j, i1, k, norm;
  double machtol, w, s, f, g, h, tm, TEMP;

  norm = 0.0;
  for (i = 1; i <= m; i++) {
    w = 0.0;
    for (j = 0; j < n; j++)
      w = fabs(a[i][j]) + w;
    if (w > norm)
      norm = w;
  }
  machtol = em[0] * norm;
  em[1] = norm;
  for (i = 1; i <= n; i++) {
    i1 = i + 1;
    s = 0.0;
    for (k = i1; k <= m; k++) {
      TEMP = a[k][i - 1];
      s = TEMP * TEMP + s;
    }
    if (s < machtol)
      d[i] = a[i][i - 1];
    else {
      f = a[i][i - 1];
      s = f * f + s;
      if (f < 0)
	g = sqrt(s);
      else
	g = -sqrt(s);
      d[i] = g;
      h = f * g - s;
      a[i][i - 1] = f - g;
      for (j = i1 - 1; j < n; j++) {
	tm = 0.0;
	for (k = i; k <= m; k++)
	  tm = a[k][i - 1] * a[k][j] + tm;
	tm /= h;
	for (norm = i; norm <= m; norm++)
	  a[norm][j] += a[norm][i - 1] * tm;
      }
    }
    if (i < n) {
      s = 0.0;
      for (k = i1; k < n; k++) {
	TEMP = a[i][k];
	s = TEMP * TEMP + s;
      }
      if (s < machtol)
	b[i] = a[i][i1 - 1];
      else {
	f = a[i][i1 - 1];
	s = f * f + s;
	if (f < 0)
	  g = sqrt(s);
	else
	  g = -sqrt(s);
	b[i] = g;
	h = f * g - s;
	a[i][i1 - 1] = f - g;
	for (j = i1; j <= m; j++) {
	  tm = 0.0;
	  for (k = i1 - 1; k < n; k++)
	    tm = a[i][k] * a[j][k] + tm;
	  tm /= h;
	  for (norm = i1 - 1; norm < n; norm++)
	    a[j][norm] += a[i][norm] * tm;
	}
      }
    }
  }
}  /*HshReaBid*/


static void PstTfmMat(double **a,int n,double **v,double *b)
{
  int i, i1, j, k, l;
  double h, tm;

  i1 = n;
  v[n][n - 1] = 1.0;
  for (i = n - 1; i >= 1; i--) {
    h = b[i] * a[i][i1 - 1];
    if (h < 0) {
      for (j = i1; j <= n; j++)
	v[j][i - 1] = a[i][j - 1] / h;
      for (j = i1 - 1; j < n; j++) {
	tm = 0.0;
	for (k = i1; k <= n; k++)
	  tm = a[i][k - 1] * v[k][j] + tm;
	for (l = i1; l <= n; l++)
	  v[l][j] += v[l][i - 1] * tm;
      }
    }
    for (j = i1; j <= n; j++) {
      v[i][j - 1] = 0.0;
      v[j][i - 1] = 0.0;
    }
    v[i][i - 1] = 1.0;
    i1 = i;
  }
}  /*PstTfmMat*/


static void PreTfmMat(double **a,int m,int n,double *d)
{
  int i, i1, j, k, l;
  double g, h, tm;

  for (i = n; i >= 1; i--) {
    i1 = i + 1;
    g = d[i];
    h = g * a[i][i - 1];
    for (j = i1 - 1; j < n; j++)
      a[i][j] = 0.0;
    if (h < 0) {
      for (j = i1 - 1; j < n; j++) {
	tm = 0.0;
	for (k = i1; k <= m; k++)
	  tm = a[k][i - 1] * a[k][j] + tm;
	tm /= h;
	for (l = i; l <= m; l++)
	  a[l][j] += a[l][i - 1] * tm;
      }
      for (j = i; j <= m; j++)
	a[j][i - 1] /= g;
    } else {
      for (j = i; j <= m; j++)
	a[j][i - 1] = 0.0;
    }
    a[i][i - 1]++;
  }
}  /*PreTfmMat*/


static int QriSngValDecBid(double *d,double *b,int m,int n,double **a,double **v,double *em)
{
  int n0, n1, k, k1, i, i1, count, max, rnk, l;
  double tol, bmax, z, x, y, g, h, f, c, s, min, u, w;

  tol = em[2] * em[1];
  count = 0;
  bmax = 0.0;
  max = (int)floor(em[4] + 0.5);
  min = em[6];
  rnk = n;
  n0 = n;
_Lgoin:
  k = n;
  n1 = n - 1;
_Lnext:
  k--;
  if (k > 0) {
    if (fabs(b[k]) >= tol) {
      if (fabs(d[k]) >= tol)
	goto _Lnext;
      c = 0.0;
      s = 1.0;
      for (i = k; i <= n1; i++) {
	f = s * b[i];
	b[i] = c * b[i];
	i1 = i + 1;
	if (fabs(f) < tol)
	  goto _Lneglect;
	g = d[i1];
	h = sqrt(f * f + g * g);
	d[i1] = h;
	c = g / h;
	s = -(f / h);
	for (l = 1; l <= m; l++) {
	  u = a[l][k - 1];
	  w = a[l][i1 - 1];
	  a[l][k - 1] = u * c + w * s;
	  a[l][i1 - 1] = w * c - u * s;
	}
      }
_Lneglect: ;
    } else if (fabs(b[k]) > bmax)
      bmax = fabs(b[k]);
  }
  if (k == n1) {
    if (d[n] < 0) {
      d[n] = -d[n];
      for (i = 1; i <= n0; i++)
	v[i][n - 1] = -v[i][n - 1];
    }
    if (d[n] <= min)
      rnk--;
    n = n1;
  } else {
    count++;
    if (count > max)
      goto _Leind;
    k1 = k + 1;
    z = d[n];
    x = d[k1];
    y = d[n1];
    if (n1 == 1)
      g = 0.0;
    else
      g = b[n1 - 1];
    h = b[n1];
    f = ((y - z) * (y + z) + (g - h) * (g + h)) / (2 * h * y);
    g = sqrt(f * f + 1);
    if (f < 0)
      u = f - g;
    else
      u = f + g;
    f = ((x - z) * (x + z) + h * (y / u - h)) / x;
    c = 1.0;
    s = 1.0;
    for (i = k1; i < n; i++) {
      i1 = i;
      g = b[i1];
      y = d[i + 1];
      h = s * g;
      g = c * g;
      z = sqrt(f * f + h * h);
      c = f / z;
      s = h / z;
      if (i1 != k1)
	b[i1 - 1] = z;
      f = x * c + g * s;
      g = g * c - x * s;
      h = y * s;
      y *= c;
      for (l = 1; l <= n0; l++) {
	u = v[l][i1 - 1];
	w = v[l][i];
	v[l][i1 - 1] = u * c + w * s;
	v[l][i] = w * c - u * s;
      }
      z = sqrt(f * f + h * h);
      d[i1] = z;
      c = f / z;
      s = h / z;
      f = c * g + s * y;
      x = c * y - s * g;
      for (l = 1; l <= m; l++) {
	u = a[l][i1 - 1];
	w = a[l][i];
	a[l][i1 - 1] = u * c + w * s;
	a[l][i] = w * c - u * s;
      }
    }
    b[n1] = f;
    d[n] = x;
  }
  if (n > 0)
    goto _Lgoin;
_Leind:
  em[3] = bmax;
  em[5] = count;
  em[7] = rnk;
  return n;
}  /*QriSngValDecBid*/


static int QriSngValDec(double **a,int m,int n,double *val,double **v,double *em)
{
  double *b;
  int i;
 
  b=(double *)m_memory((void *)NULL,(1+n)*sizeof(double),PROGNAME);
  HshReaBid(a, m, n, val, b, em);
  PstTfmMat(a, n, v, b);
  PreTfmMat(a, m, n, val);
  i=(QriSngValDecBid(val, b, m, n, a, v, em));

  free(b);
  return  i;
}


/* Local variables for Marquardt: */
struct LOC_Marquardt {
  int m, n;
  double *par;
} ;

void PrintPar(LINK)
struct LOC_Marquardt *LINK;
{
  int i, FORLIM;

  FORLIM = LINK->n;
  if (it == 0){
	  fprintf(stderr,"\n---------------------------------------------------------\n");
	  fprintf(stderr,"iteration\tresidual");
	  for (i =1; i <=FORLIM;i++)
		  fprintf(stderr,"\tpar[%d]",i);
	  fprintf(stderr,"\n---------------------------------------------------------\n");
  }
  fprintf(stderr,"%d\t\t%f\t",it,sqrt(fpar));
  for (i = 1; i <= FORLIM; i++){
    if (!fixed[i])
    fprintf(stderr,"%f\t", LINK->par[i]);
    else
    fprintf(stderr,"*%f\t", LINK->par[i]);
  }
  fprintf(stderr,"\n");
}

double VecVecN(a, b, LINK)
double *a, *b;
struct LOC_Marquardt *LINK;
{
  int k;
  double s;
  int FORLIM;

  s = 0.0;
  FORLIM = LINK->n;
  for (k = 1; k <= FORLIM; k++)
    s = a[k] * b[k] + s;
  return s;
}

double VecVecM(a, b, LINK)
double *a, *b;
struct LOC_Marquardt *LINK;
{
  int k;
  double s;
  int FORLIM;

  s = 0.0;
  FORLIM = LINK->m;
  for (k = 1; k <= FORLIM; k++)
    s = a[k] * b[k] + s;
  return s;
}

/*-
 * int Marquardt(int m, int n, double *par, double *g, double **v,
 *      double *inp, double *out)
 * 	m	number of points
 *	n	number of parameters
 *	par	array with parameters (n+1)
 *	g	array containing residuals (m+1)
 *	v	matrix (m+1*n+1)
 *	inp	input parameters (array 8)
 *	out	output (array 8)
 */

int Marquardt(int m_,int n_,double *par_,double *g,double **v,double *inp,double *out)
{
  struct LOC_Marquardt V;
  int maxfe, fe, i, j, k, err;
  double s, vv, ww, w, mu, res,  fparpres, lambdamin, pw,
	 reltolres, abstolres;
  double lambda=0.0;
  double *em;
  double *val, *b, *bb, *parpres;
  double **jac;
  double TEMP;
  int p;
  int FORLIM, FORLIM1, FORLIM2;

  /* Allocate memory for arrays */
  em=(double *)m_memory((void *)NULL,8*sizeof(double),PROGNAME);
  val=(double *)m_memory((void *)NULL,(1+n_)*sizeof(double),PROGNAME);
  b=(double *)m_memory((void *)NULL,(n_+1)*sizeof(double),PROGNAME);
  bb=(double *)m_memory((void *)NULL,(1+n_)*sizeof(double),PROGNAME);
  parpres=(double *)m_memory((void *)NULL,(1 + n_)*sizeof(double),PROGNAME);
  jac=m_matr(m_+1,n_+1);
  V.par = par_;
  V.m = m_; V.n = n_;
  w = 0.5;
  mu = 0.01;
  if (inp[6] < 1e-7)
    ww = 1e-8;
  else
    ww = 0.1 * inp[6];
  vv = 10.0;
  em[0] = inp[0];
  em[2] = inp[0];
  em[6] = inp[0];
  em[4] = n_ * 10.0;
  reltolres = inp[3];
  TEMP = inp[4];
  abstolres = TEMP * TEMP;
  maxfe = (int)floor(inp[5] + 0.5);
  err = 0;
  fe = 1;
  it = 1;
  p = 0;
  fpar = 0.0;
  res = 0.0;
  pw = log(ww * inp[0]) / -2.3;
  if (Funct(m_, n_, par_, g) != 0) {
    err = 3;
    goto _Lescape;
  }
  fpar = VecVecM(g, g, &V);
  out[3] = sqrt(fpar);
  it = 0;
  if (maqverb)
	PrintPar(&V);
  do {
    it++;
    Jacobian(m_, n_, par_, g, jac);
    i = QriSngValDec(jac, m_, n_, val, v, em);
    if (it == 1)
      lambda = inp[6] * VecVecN(val, val, &V);
    else if (p == 0)
      lambda *= w;
    else
      p = 0;
    FORLIM = n_;
    for (i = 1; i <= FORLIM; i++) {
      s = 0.0;
      FORLIM1 = m_;
      for (k = 1; k <= FORLIM1; k++)
	s = jac[k][i - 1] * g[k] + s;
      b[i] = val[i] * s;
    }
_Ll:
    FORLIM = n_;
    for (i = 1; i <= FORLIM; i++) {
      TEMP = val[i];
      bb[i] = b[i] / (TEMP * TEMP + lambda);
    }
    FORLIM = n_;
    for (i = 1; i <= FORLIM; i++) {
      if (!fixed[i]){
      s = 0.0;
      FORLIM1 = n_;
      for (k = 1; k <= FORLIM1; k++)
	s = v[i][k - 1] * bb[k] + s;
      parpres[i] = par_[i] - s;
     }
    }
    fe++;
    if (fe >= maxfe)
      err = 1;
    else if (Funct(m_, n_, parpres, g) != 0)
      err = 2;
    if (err != 0)
      goto _Lexit;
    fparpres = VecVecM(g, g, &V);
    res = fpar - fparpres;
    if (res < mu * VecVecN(b, bb, &V)) {
      p++;
      lambda = vv * lambda;
      if (p == 1) {
	lambdamin = ww * VecVecN(val, val, &V);
	if (lambda < lambdamin)
	  lambda = lambdamin;
      }
      if ((double)p < pw){
	goto _Ll;
      }else {
	err = 4;
	goto _Lexit;
      }
    }
    for (i =0; i<=n_;i++)
      par_[i]=parpres[i];
    /*    memcpy(V.par, parpres, (n_+1)*sizeof(double));*/
    fpar = fparpres;
    if(maqverb)
	PrintPar(&V);
  } while (fpar > abstolres && res > reltolres * fpar + abstolres);
_Lexit:
  FORLIM = n_;
  for (i = 1; i <= FORLIM; i++) {
    FORLIM1 = n_;
    for (j = 1; j <= FORLIM1; j++)
      jac[j][i - 1] = v[j][i - 1] / (val[i] + inp[0]);
  }
  FORLIM = n_;
  for (i = 1; i <= FORLIM; i++) {
    for (j = 1; j <= i; j++) {
      s = 0.0;
      FORLIM2 = n_;
      for (k = 0; k < FORLIM2; k++)
	s = jac[i][k] * jac[j][k] + s;
      v[i][j - 1] = s;
      v[j][i - 1] = s;
    }
  }
  lambda = val[1];
  lambdamin = lambda;
  FORLIM = n_;
  for (i = 2; i <= FORLIM; i++) {
    if (val[i] > lambda)
      lambda = val[i];
    else if (val[i] < lambdamin)
      lambdamin = val[i];
  }
  TEMP = lambda / (lambdamin + inp[0]);
  out[7] = TEMP * TEMP;
  out[2] = sqrt(fpar);
  out[6] = sqrt(fabs(res + fpar)) - out[2];
_Lescape:
  out[4] = fe;
  out[5] = it - 1.0;
  out[1] = err;
  if (maqverb){
  	switch ((int)out[1]){
	case 0:
		(void)fprintf(stderr,"marquardt: Normal termination\n");
		break;
	case 1:
		(void)fprintf(stderr,"marquardt: function calls greater than %d\n",(int)inp[5]);
		break;
	case 2:
		(void)fprintf(stderr,"marquardt: function call returned false\n");
		break;
	case 3:
		(void)fprintf(stderr,"marquardt: func became false with initial parameters\n");
		break;
	case 4:
		(void)fprintf(stderr,"marquardt: asked precision could not be obtained\n");
		break;
	default:
		break;
	}
  }
	 
  free(em);
  free(val);
  free(b);
  free(bb);
  free(parpres);
  m_free_matr(jac,m_+1);

  return out[1];
}  /*Marquardt*/


/*-
 * int maqinit (int *m, int *n)
 *
 *  returns -1 on error
 */
#ifdef HAVE_LIBSLANG
SLuser_Object_Type *rv_a,*par_a,*inp_a,*fixed_a;
#endif

int
maqinit(int *m,int *n)
{
	int i;
	static int add = 0;
	int noslang = 1;
	static int max_m = 32000, max_n = 15;

#ifdef HAVE_LIBSLANG
	noslang=0;
#endif

  if (*m < MINM){
	  fprintf(stderr,"error: m < %d\n",MINM);
	  return -1;
  }

  if (*m > max_m || *n > max_n){
	  fprintf(stderr,"error: n or m to large (%d > %d || %d > %d)\n",*n,max_n,*m,max_m);
	  return -1;
  }
  if (out != NULL){
	  fprintf(stderr,"error: marquardt already initialized: (m = %d, n = %d)\n",maq_m,maq_n);
	  fprintf(stderr,"use maqend first\n");
	  return -1;
  }

  maq_n = *n > max_n ? max_n : *n;
  maq_m = *m > max_m ? max_m : *m;

  if (!add || noslang){
	  max_m = maq_m;
	  max_n = maq_n;
	  par=(double *)m_memory(NULL,(maq_n + 2)*sizeof(double),PROGNAME);
	  fixed=(int *)m_memory(NULL,(maq_n+1)*sizeof(int),PROGNAME);
	  rv=(double *)m_memory(NULL,(maq_m + 1)*sizeof(double),PROGNAME);
	  inp=(double *)m_memory(NULL,8*sizeof(double),PROGNAME);
  }
  out=(double *)m_memory(NULL,8*sizeof(double),PROGNAME);
  jjinv=m_matr(maq_m+1,maq_n+1);


  inp[0] = DBL_EPSILON;
  inp[3] = 1e-4;
  inp[4] = 1e-3;
  inp[5] = 50.0;
  inp[6] = 0.01;
  for (i=0; i <= maq_n;i++)
	fixed[i]=0;

#ifdef HAVE_LIBSLANG
  allfunc = sluser; /* set fit fucntion to S-Lang user supplied */
  SLang_Traceback = 1;

/* Add all these arrays only once ...*/

  if (!add){
  add++;
  if (NULL == (rv_a = SLang_add_array ("rv",   /* slang name */
                                 (long *)rv,   /* location of the array */
                                 1,      /* number of dimensions */
                                 *m + 1,    /* number of elements in X direction */
                                 0, 0,   /* Number in Y and Z directions */
                                 'f',    /* FLoating point array */
                                 SLANG_IVARIABLE))){   /* Read/Write array */
    fprintf(stderr,"Failed to add array rv");
    return -1;
  }
  if (NULL == (par_a = SLang_add_array ("par",   /* slang name */
                                 (long *)par,   /* location of the array */
                                 1,      /* number of dimensions */
                                 *n + 1,    /* number of elements in X direction */
                                 0, 0,   /* Number in Y and Z directions */
                                 'f',    /* FLoating point array */
                                 SLANG_IVARIABLE))){   /* Read/Write array */
    fprintf(stderr,"Failed to add array par");
    return -1;
  }
  if (NULL == (inp_a = SLang_add_array ("inp",   /* slang name */
                                 (long *)inp,   /* location of the array */
                                 1,      /* number of dimensions */
                                 8,    /* number of elements in X direction */
                                 0, 0,   /* Number in Y and Z directions */
                                 'f',    /* FLoating point array */
                                 SLANG_IVARIABLE))){   /* Read/Write array */
    fprintf(stderr,"Failed to add array inp");
    return -1;
  }
  if (NULL == (fixed_a = SLang_add_array ("fixed",   /* slang name */
                                 (long *)fixed,   /* location of the array */
                                 1,      /* number of dimensions */
                                 *n + 1,    /* number of elements in X direction */
                                 0, 0,   /* Number in Y and Z directions */
                                 'i',    /* FLoating point array */
                                 SLANG_IVARIABLE))){   /* Read/Write array */
    fprintf(stderr,"Failed to add array fixed");
    return -1;
  }
  }

#endif

  return 0;
}

void
maqend(int *m)
{
	int noslang = 1;

#ifdef HAVE_LIBSLANG
	noslang = 0;
#endif

  if (out == NULL){
		fprintf(stderr,"error: maq not initialized, use maqinit first.\n");
		return;
  }else{
	  if (noslang){
		  free(par);
		  free(fixed);
		  free(rv);
		  free(inp);
		  par = NULL;
		  fixed = NULL;
		  rv = NULL;
		  inp = NULL;
	  }
	  free(out);
	  m_free_matr(jjinv,*m+1);
	  out = NULL;
	  jjinv = NULL;
  }
}

void
prhead()
{
	if (inp == NULL){
		fprintf(stderr,"error: use maqinit first.\n");
		return;
	}else{
		fprintf(stderr,"marquardt: input parameters:\n");
		fprintf(stderr,"inp[0] = %g (Machine precision)\n",inp[0]);
		fprintf(stderr,"inp[3] = %g (relative tolerance for the difference\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[3]);
		fprintf(stderr,"inp[4] = %g (absolut tolerance for the difference\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[4]);
		fprintf(stderr,"inp[5] = %g (The maximum number of call to funct allowed)\n",inp[5]);
		fprintf(stderr,"inp[6] = %g (A starting value used for the relation between\n\t the gradient and the gaus-newton direction)\n",inp[6]);
	}
}

void
prtail()
{
	if (inp == NULL){
		fprintf(stderr,"error: use maqinit first.\n");
		return;
	}else{
		fprintf(stderr,"marquardt: output parameters:\n");
		fprintf(stderr,"out[1] = %g (process exit code)\n",out[1]);
		fprintf(stderr,"out[2] = %g (The euclidean norm of the residual vector\n\tcalculated with values of the unknows delivered)\n",out[2]);
		fprintf(stderr,"out[3] = %g (The euclidean norm of the residual vector\n\tcalculated with  the initial values of the unknows variables)\n",out[3]);
		fprintf(stderr,"out[4] = %g (The number of calls to func performed)\n",out[4]);
		fprintf(stderr,"out[5] = %g (The total number of iterations performed)\n",out[5]);
		fprintf(stderr,"out[6] = %g (The improvement of the euclidean norm of the\n\tresidual vector in the last iteration step)\n",out[6]);
		fprintf(stderr,"out[7] = %g (The condition number of j' * j. I.E. The ratio\n\t of its largest to smallest eigenvalues)\n",out[7]);
	}
}


int maqrun()
{
	if (inp == NULL){
		fprintf(stderr,"error: use maqinit first.\n");
		return -1;
	}else
		return Marquardt(maq_m, maq_n, par,rv, jjinv, inp, out);
}

@


1.6
log
@Version: 0.99g
@
text
@d1 1
a1 1
/* $Header: /home/schjpriv/schj/src/vamps_0.99e/src/maq.lib/RCS/marquard.c,v 1.5 1997/09/09 08:42:42 schj Exp $ */
d4 1
a4 1
 *  $Date: 1997/09/09 08:42:42 $
@


1.5
log
@0.99e version
@
text
@d1 1
a1 1
/* $Header: /home/schj/src/vamps_0.99/src/maq/RCS/marquard.c,v 1.4 1996/11/05 15:07:59 schj Alpha $ */
d4 1
a4 1
 *  $Date: 1996/11/05 15:07:59 $
@


1.4
log
@Updated to Alpha version
@
text
@d1 1
a1 1
/* $Header: /home/schj/src/vamps_0.96/src/maq/RCS/marquard.c,v 1.3 1995/12/17 21:59:50 schj Exp $ */
d4 1
a4 1
 *  $Date: 1995/12/17 21:59:50 $
d11 1
d13 14
a26 8

double **maqdata;
double *inp;
double *out;
double **jjinv;
double *par;
double *fixed;
double *rv;
d28 1
a28 1
int	maqverb=0;
d32 57
d91 1
a91 1
Jacobian(int m,int n,double *par,double *rv,double **jac)
d97 1
a97 1
  rvcopy=(double *)memory((void *)NULL,(m+1)*sizeof(double),PROGNAME);
d103 3
a105 3
    d = par[j] * diffac;
    p = par[j];
    par[j] = d;
d107 3
a109 3
    d = par[j];
    p = par[j];
    par[j] = d;
d111 1
a111 1
    if (!Funct(m, n, par, rv))
d113 1
a113 1
    par[j] = p;
d392 1
a392 1
  b=(double *)memory((void *)NULL,(1+n)*sizeof(double),PROGNAME);
d415 8
a422 1
  fprintf(stderr,"\n%s: it: %d fpar= %f\t",PROGNAME,it,sqrt(fpar));
d463 1
a463 1
 * void Marquardt(int m, int n, double *par, double *g, double **v,
d474 1
a474 1
void Marquardt(int m_,int n_,double *par_,double *g,double **v,double *inp,double *out)
d489 6
a494 9
  em=(double *)memory((void *)NULL,8*sizeof(double),PROGNAME);
  val=(double *)memory((void *)NULL,(1+n_)*sizeof(double),PROGNAME);
  b=(double *)memory((void *)NULL,(n_+1)*sizeof(double),PROGNAME);
  bb=(double *)memory((void *)NULL,(1+n_)*sizeof(double),PROGNAME);
  parpres=(double *)memory((void *)NULL,(1+n_)*sizeof(double),PROGNAME);
  jac=matr(m_+1,n_+1);

  V.m = m_;
  V.n = n_;
d496 1
d507 1
a507 1
  em[4] = V.n * 10.0;
d519 1
a519 1
  if (!Funct(V.m, V.n, V.par, g)) {
d530 2
a531 2
    Jacobian(V.m, V.n, V.par, g, jac);
    i = QriSngValDec(jac, V.m, V.n, val, v, em);
d538 1
a538 1
    FORLIM = V.n;
d541 1
a541 1
      FORLIM1 = V.m;
d547 1
a547 1
    FORLIM = V.n;
d552 1
a552 1
    FORLIM = V.n;
d556 1
a556 1
      FORLIM1 = V.n;
d559 1
a559 1
      parpres[i] = V.par[i] - s;
d565 1
a565 1
    else if (!Funct(V.m, V.n, parpres, g))
d586 2
a587 2
    for (i =0; i<=n_+1;i++)
      V.par[i]=parpres[i];
d594 1
a594 1
  FORLIM = V.n;
d596 1
a596 1
    FORLIM1 = V.n;
d600 1
a600 1
  FORLIM = V.n;
d604 1
a604 1
      FORLIM2 = V.n;
d613 1
a613 1
  FORLIM = V.n;
a648 2
 if (maqverb)
         PrintPar(&V);
d655 3
a657 1
  free_matr(jac,m_+1);
d662 1
a662 1
 * int maqinit (int m, int n)
d664 1
a664 1
 *  returns 1 on error
d666 4
d671 1
a671 1
maqinit(int m,int n)
d673 37
a709 1
int i;
a710 2
  if (m < MINM)
  	return 1;
d712 2
a713 8
  par=(double *)memory((void *)NULL,(1+n)*sizeof(double),PROGNAME);
  fixed=(double *)memory((void *)NULL,(1+n)*sizeof(double),PROGNAME);
  rv=(double *)memory((void *)NULL,(1+m)*sizeof(double),PROGNAME);
  inp=(double *)memory((void *)NULL,8*sizeof(double),PROGNAME);
  out=(double *)memory((void *)NULL,8*sizeof(double),PROGNAME);
  jjinv=matr(m+1,n+1);
  inp[0] = 1e-12;
  inp[3] = 1e-3;
d717 1
a717 1
  for (i=0; i <= n;i++)
d719 53
d776 1
a776 1
maqend(int m)
d778 25
a802 6
  free_matr(jjinv,m+1);
  free(par);
  free(fixed);
  free(inp);
  free(out);
  free(rv);
d808 11
a818 6
  	fprintf(stderr,"marquardt: input parameters:\n");
	fprintf(stderr,"inp[0] = %g (Machine precision)\n",inp[0]);
	fprintf(stderr,"inp[3] = %g (relative tolerance for the difference\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[3]);
	fprintf(stderr,"inp[4] = %g (absolut tolerance for the difference\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[4]);
	fprintf(stderr,"inp[5] = %g (The maximum number of call to funct allowed)\n",inp[5]);
	fprintf(stderr,"inp[6] = %g (A starting value used for the relation between\n\t the gradient and the gaus-newton direction)\n",inp[6]);
d824 13
a836 8
  	fprintf(stderr,"marquardt: output parameters:\n");
	fprintf(stderr,"out[1] = %g (process exit code)\n",out[1]);
	fprintf(stderr,"out[2] = %g (The euclidean norm of the residual vector\n\tcalculated with values of the unknows delivered)\n",out[2]);
	fprintf(stderr,"out[3] = %g (The euclidean norm of the residual vector\n\tcalculated with  the initial values of the unknows variables)\n",out[3]);
	fprintf(stderr,"out[4] = %g (The number of calls to func performed)\n",out[4]);
	fprintf(stderr,"out[5] = %g (The total number of iterations performed)\n",out[5]);
	fprintf(stderr,"out[6] = %g (The improvement of the euclidean norm of the\n\tresidual vector in the last iteration step)\n",out[6]);
	fprintf(stderr,"out[7] = %g (The condition number of j' * j. I.E. The ratio\n\t of its largest to smallest eigenvalues)\n",out[7]);
d838 11
@


1.3
log
@added output statistics
@
text
@d1 2
a2 2
/* $Header: /home/schj/src/vamps_0.91/maq/RCS/marquard.c,v 1.2 1995/11/14 20:37:35 schj Exp $ */
/*- $RCSfile: marquard.c,v $
d4 1
a4 5
 *  $Date: 1995/11/14 20:37:35 $
 *  $Log: marquard.c,v $
 *  Revision 1.2  1995/11/14 20:37:35  schj
 *  added RCS header
 *
d22 1
a22 3


#define diffac          1.01
d26 2
a27 5
static void Jacobian(m, n, par, rv, jac)
int m, n;
double *par;
double *rv;
double **jac;
d48 1
a48 1
      fprintf(stderr,"marquardt.c: function evaluation error\n");
a61 1
#undef diffac
d64 1
a64 5
static void HshReaBid(a, m, n, d, b, em)
double **a;
int m, n;
double *d, *b;
double *em;
d139 1
a139 5
static void PstTfmMat(a, n, v, b)
double **a;
int n;
double **v;
double *b;
d169 1
a169 4
static void PreTfmMat(a, m, n, d)
double **a;
int m, n;
double *d;
d200 1
a200 6
static int QriSngValDecBid(d, b, m, n, a, v, em)
double *d, *b;
int m, n;
double **a;
double **v;
double *em;
d323 1
a323 6
static int QriSngValDec(a, m, n, val, v, em)
double **a;
int m, n;
double *val;
double **v;
double *em;
d351 1
a351 1
  fprintf(stderr,"marquardt: it: %d\t fpar= %f\t",it,sqrt(fpar));
d403 1
a403 6
void Marquardt(m_, n_, par_, g, v, inp, out)
int m_, n_;
double *par_;
double *g;
double **v;
double *inp, *out;
d517 3
a519 1
    memcpy(V.par, parpres, (n_+1)*sizeof(double));
d562 1
a562 1
		fprintf(stderr,"marquardt: Normal termination\n");
d565 1
a565 1
		fprintf(stderr,"marquardt: function calls greater than %d\n",(int)inp[5]);
d568 1
a568 1
		fprintf(stderr,"marquardt: function call returned false\n");
d571 1
a571 1
		fprintf(stderr,"marquardt: func became false with initial parameters\n");
d574 1
a574 1
		fprintf(stderr,"marquardt: asked precision could not be obtained\n");
d580 3
a582 1
  
d591 8
a598 4
void
maqinit(m,n)
int m;
int n;
d602 3
d610 1
a610 1
  jjinv=matr(m,n);
d614 1
a614 1
  inp[5] = 375.0;
d618 1
d622 1
a622 2
maqend(m)
int m;
d624 1
a624 1
  free_matr(jjinv,m);
d637 2
a638 2
	fprintf(stderr,"inp[3] = %g (relative tolerance for the diffrence\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[3]);
	fprintf(stderr,"inp[4] = %g (absolut tolerance for the diffrence\n\tbetween the euclidean norm of the ultimate and penultimate\n\tresidual vector)\n",inp[4]);
d640 1
a640 1
	fprintf(stderr,"inp[6] = %g (A starting value used fro the relation between\n\t the gradient and the gaus-newton direction)\n",inp[6]);
@


1.2
log
@added RCS header
@
text
@d1 2
a2 2
/* $Header: /homes/schj/src/vamps_0.91/RCS/vamps.c,v 0.20 1995/10/30 13:59:49 schj Exp $ */
/*- $RCSfile: vamps.c,v $
d4 5
a8 2
 *  $Date: 1995/10/30 13:59:49 $
 *  $Log: vamps.c,v $
d22 1
d47 1
d51 5
d60 1
d62 2
d383 2
a384 1
  for (i = 1; i <= FORLIM; i++)
d386 3
d521 1
d527 1
a545 3
#ifdef DEBUG
fprintf(stderr,"marquardt: p = %d pw = %g\n",p,pw);
#endif
d593 21
d628 2
d631 1
d641 2
d649 7
d657 3
a659 1
  if (maqverb){
d666 1
d668 3
a678 26

  	switch ((int)out[1]){
	case 0:
		fprintf(stderr,"marquardt: Normal termination\n");
		break;
	case 1:
		fprintf(stderr,"marquardt: function calls greater than %d\n",(int)inp[5]);
		break;
	case 2:
		fprintf(stderr,"marquardt: function call returned false\n");
		break;
	case 3:
		fprintf(stderr,"marquardt: func became false with initial parameters\n");
		break;
	case 4:
		fprintf(stderr,"marquardt: asked precision could not be obtained\n");
		break;
	default:
		break;
	}
  }
  free_matr(jjinv,m);
  free(par);
  free(inp);
  free(out);
  free(rv);
@


1.1
log
@Initial revision
@
text
@d1 5
a5 3
/*
 * marquard.c
 *
@
